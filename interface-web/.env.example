# Copie para .env dentro de interface-web/ e ajuste se necessário.
# O Vite só lê .env do próprio projeto — o .env da raiz do repositório NÃO
# vale aqui (era uma das causas de configuração divergente).

# API do assistente (python -m holder.interfaces.api).
VITE_BACKEND_URL=http://localhost:8000/api

# Dashboard embutido no iframe (streamlit run holder/interfaces/dashboard/app.py).
VITE_DASHBOARD_URL=http://localhost:8501
