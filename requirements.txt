# Dashboard (app.py) — lê do SQL Server local (banco "holder", populado por
# ingestao.py), não mais do .xlsx direto. Instância esperada: localhost\SQLEXPRESS
# (SQL Server Express + "ODBC Driver 17 for SQL Server", autenticação Windows).
streamlit
pandas
plotly
openpyxl
sqlalchemy
pyodbc
matplotlib

# Score de risco de cancelamento (modelo_risco.py) — regressão logística
# treinada/validada contra a base real (ver testar_modelo_risco.py)
scikit-learn
numpy

# Testes (testes/, rodar com `pytest` na raiz) — `pythonpath` no pytest.ini
# exige pytest >= 7
pytest>=7

# Backend do fallback de IA do Assistente de Consultas (server.py)
Flask>=3.1
flask-cors>=6.0
groq>=1.7
python-dotenv>=1.2
