# Dashboard (streamlit run holder/interfaces/dashboard/app.py) — lê do SQL
# Server local (banco "holder", populado por `python -m holder.infra.etl.ingestao`),
# não do .xlsx direto. Instância esperada: localhost
# (SQL Server Express + "ODBC Driver 17 for SQL Server", autenticação Windows).
streamlit
pandas
plotly
openpyxl
sqlalchemy
pyodbc
matplotlib

# Score de risco de cancelamento (python -m holder.aplicacao.treino) —
# regressão logística treinada/validada contra a base real
# (ver testes/test_modelo_risco.py)
scikit-learn
numpy

# Testes (testes/, rodar com `pytest` na raiz) — `pythonpath` no pytest.ini
# exige pytest >= 7
pytest>=7

# API do assistente (python -m holder.interfaces.api)
Flask>=3.1
flask-cors>=6.0
groq>=1.7
python-dotenv>=1.2
