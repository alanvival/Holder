# Segredos — nunca versionar
.env
.env.local
interface-web/.env
interface-web/.env.local

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# Node
node_modules/
dist/

# Artefatos gerados em tempo de execução: banco do admin, modelo treinado e
# logs. Tudo aqui se recria — o banco na primeira subida da API, o modelo com
# `python -m holder.aplicacao.treino`. A planilha do desafio, que é ENTRADA e
# não artefato, fica em dados/ e é versionada.
dados/gerado/
